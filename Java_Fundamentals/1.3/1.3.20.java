// ommited steps for simplicity


class removeNode {

    private class Node
    {
        String item;
        Node next;
    }

    public void static main(String args[])
    {
        int k; // k in the requirement
        int p; // count from 0

        Node.first = x;

        if(p=0; x.next != nil; p++)
            x = x.next;
            if(p == k)
                return p;
    }
}